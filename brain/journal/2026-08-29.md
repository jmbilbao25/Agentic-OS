---
date: 2026-08-29
---

# 2026-08-29

- 02:36Z — Made AgentOS harness-agnostic: kernel moved to AGENTS.md, per-vendor bindings generated by adapters/install.sh (claude, kiro, cursor, copilot, windsurf, gemini, agents.md), cloud-config -> config/. selftest gained a binding-drift check.
- 02:36Z — Vault restructured into raw -> wiki -> output layers with bin/os capture and bin/os ship as entry points. Renamed the three vendor-named notes and rewrote every inbound wikilink; journal left untouched because it is append-only.
- 02:36Z — Built server/: FastAPI + hybrid retrieval (SQLite FTS5/BM25 + sqlite-vec, weighted RRF), local quantised BGE embeddings, Google OAuth with a single-email allowlist, and an interactive ARMS orbit UI with no framework or CDN.
- 02:36Z — Measured retrieval instead of assuming it. Found three real bugs: fastembed query_embed is a no-op for bge-small (cosine 1.0000 vs embed) so the BGE instruction prefix is now applied explicitly; FTS5's porter tokenizer keeps stopwords, which buried 'Git Is The Disk' under its own title words; unweighted RRF scored worse than semantic alone on a small corpus. Tuned to RRF_K=20 / W_SEMANTIC=2.0 — hybrid now beats both halves on top-5 (9/10).
- 02:36Z — Verified the UI by screenshotting it headless rather than trusting the source. Caught the canvas being measured before layout, which had rendered the whole map into a 300x150 corner, plus faint ring bands, a label pile-up, and a banner covering a ring label.
- 02:36Z — Wrote deploy/ for a t3.micro (~$10/mo): swap, venv, systemd unit with MemoryMax, a 15-minute pull+reindex timer, and Tailscale Funnel for real TLS with no domain purchase and no inbound port.
- 06:03Z — Replaced Google OAuth with single-user credential auth: PBKDF2-HMAC-SHA256 (600k, hashlib), CSRF token on the login form, per-address lockout, session cleared on login to stop fixation, and a local-path-only 'next'. Dropped authlib entirely, and avoided python-multipart by parsing the one urlencoded form with urllib.parse.parse_qs.
- 06:03Z — Caught a username-enumeration timing leak in my own auth before shipping it: verifying against a cheap-iteration decoy hash made a wrong username 12x faster to reject than a wrong password (0.008s vs 0.101s). Fix is to run the real KDF unconditionally and fold the username result in afterwards. Measured after: 0.0996s vs 0.1004s.
- 06:03Z — Fixed two bugs with one shared function. The orbit map drew the kernel and all four skills/steering nodes, but clicking any of them 404'd AND none of them were indexed, so Ask could never cite the kernel. Cause was one split: /api/graph read load()+load_system() while /api/doc and the indexer read only load(). Added vault.load_all(). Index went 15 -> 23 docs, 62 -> 121 chunks.
- 06:03Z — Found the retrieval gap the existing knobs could not reach: typing a note's NAME did not return that note. 'AGENTS.md' did not return AGENTS.md at all; 'ralph' put Ralph Loop third. BM25 indexes title but every chunk of a doc carries the same one so it cancels out, and the vectors embed heading+text only. Added search.name_affinity() + W_TITLE=0.08 — top-1 7/16 -> 12/16, no loss on paraphrase probes. A 225-config sweep found 0.08 is the smallest value reaching the plateau and nothing beats it.
- 06:03Z — Split config into boot-time (config.py) and runtime (settings.py). settings.SCHEMA generates the settings UI, so adding a knob is one Python entry and cannot drift from the form. Writes are all-or-nothing; the API key is write-only and never serialised back to the browser.
- 06:03Z — Built the gauntlet loop as a real endpoint, not just a doc: builder drafts from the vault, a separate critic with fresh context compares blind against a fetched reference with sides randomised per round, forced A/B verdict, exit on winning rather than on a round count. Bar fetching refuses private and link-local addresses because the server holds an API key.
- 06:03Z — Rebuilt the UI against a written craft floor and tested it in a real headless browser rather than trusting the source. Nine defects the source read fine on: a disabled filled button whose label was invisible, search highlighting that painted every hit white and never cleared (destroying the ring colour system), halos on every hit turning the map into overlapping bokeh, focus left in inputs so every single-key shortcut silently died, tofu boxes from glyphs missing in both the subset font and the default Linux mono, and a mobile topbar that clipped a button off the edge.
- 06:03Z — Left three runnable checks behind: server/tools/smoke.py (37 end-to-end checks through the real ASGI app, temp index, network-optional), per-module selfchecks in settings/gauntlet/passwd, and a secret scanner in bin/os selftest that greps tracked files for private keys and API tokens — because .gitignore does not help with a file added before it was ignored.
- 06:33Z — Reconciled the fork: main (gauntlet, settings, modular UI) had lost the automations, 3 skills, 6 notes and the Caddy TLS provisioning from 9 commits that never merged. Re-landed them additively onto main, ported bin/os wiring and the /api/run endpoint, and adapted distill to main's llm.complete().
- 06:33Z — Fixed 3 bugs found during the reconciliation: OpenRouter rejects a models array over 3 items so a 4-model chain 400'd every request (now batched); load_system globbed skill reference files into top-level config docs; an exact note-name query lost to notes that merely mention it (exact match is navigation, now weighted separately at 0.45 — benchmark top-1 7/16 to 12/16).
- 06:39Z — Fixed a real bug in server/passwd.py: on a pipe, input() consumed the password line as the username, so 'printf pw | python -m server.passwd' minted a credential whose username was the password. Now only prompts on a tty.
- 09:51Z — Added four map layouts (rings/rank/grid/timeline) with tweened transitions; Rank orders the line by the live search and by whatever survives the filters. Verified in-browser at 60fps.
