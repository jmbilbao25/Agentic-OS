---
date: 2026-08-29
---

# 2026-08-29

- 02:36Z — Made AgentOS harness-agnostic: kernel moved to AGENTS.md, per-vendor bindings generated by adapters/install.sh (claude, kiro, cursor, copilot, windsurf, gemini, agents.md), cloud-config -> config/. selftest gained a binding-drift check.
- 02:36Z — Vault restructured into raw -> wiki -> output layers with bin/os capture and bin/os ship as entry points. Renamed the three vendor-named notes and rewrote every inbound wikilink; journal left untouched because it is append-only.
- 02:36Z — Built server/: FastAPI + hybrid retrieval (SQLite FTS5/BM25 + sqlite-vec, weighted RRF), local quantised BGE embeddings, Google OAuth with a single-email allowlist, and an interactive ARMS orbit UI with no framework or CDN.
- 02:36Z — Measured retrieval instead of assuming it. Found three real bugs: fastembed query_embed is a no-op for bge-small (cosine 1.0000 vs embed) so the BGE instruction prefix is now applied explicitly; FTS5's porter tokenizer keeps stopwords, which buried 'Git Is The Disk' under its own title words; unweighted RRF scored worse than semantic alone on a small corpus. Tuned to RRF_K=20 / W_SEMANTIC=2.0 — hybrid now beats both halves on top-5 (9/10).
- 02:36Z — Verified the UI by screenshotting it headless rather than trusting the source. Caught the canvas being measured before layout, which had rendered the whole map into a 300x150 corner, plus faint ring bands, a label pile-up, and a banner covering a ring label.
- 02:36Z — Wrote deploy/ for a t3.micro (~$10/mo): swap, venv, systemd unit with MemoryMax, a 15-minute pull+reindex timer, and Tailscale Funnel for real TLS with no domain purchase and no inbound port.
- 03:14Z — Replaced Google OAuth with single-user password auth: PBKDF2-SHA256 (stdlib), hmac.compare_digest, per-IP lockout, sessions bound to a credential fingerprint so rotating the password invalidates them. Verified every negative path — wrong password 401, wrong username 401, lockout 429, unset password denies everyone, stale session evicted.
